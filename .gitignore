# Secrets — NEVER commit
.env

# rclone config contains OAuth tokens
config/rclone/rclone.conf

# Backrest stores the restic repo password in its config
backrest-config/

# Local restore scratch space
restore/

# Local data (should live on the SSD, not in the repo, but guard anyway)
data/
*.log
